version: '3.8'

services:
  webhook:
    build: .
    image: dockerhub-image-policy-webhook:latest
    container_name: dockerhub-webhook
    ports:
      - "5000:5000"
    environment:
      - SESSION_SECRET=change_this_in_production
    restart: unless-stopped